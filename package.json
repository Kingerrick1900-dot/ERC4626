{
  "name": "kesov-seed-liquidity",
  "version": "1.0.0",
  "private": true,
  "description": "KESOV Mainnet Activation — L1 eUSD/WETH pool seed + Base PSM capitalize",
  "scripts": {
    "seed:l1": "hardhat run scripts/seedEthPool.ts --network mainnet",
    "seed:pool": "hardhat run scripts/seedEthPool.ts --network base",
    "seed:psm": "hardhat run scripts/seedBasePsm.ts --network base",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.0",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "ethers": "^6.13.4"
  }
}
