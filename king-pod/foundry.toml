[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true
remappings = ["forge-std/=lib/forge-std/src/"]
# Incomplete pulls from sibling ZK/elepan branches — exclude until sources land.
skip = ["test/CrownZkHardening.t.sol", "test/ZkReserves.t.sol"]
