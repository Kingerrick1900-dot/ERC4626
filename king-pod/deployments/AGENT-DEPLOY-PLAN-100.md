# AGENT DEPLOY — plan (100 words)

**Mode:** plan speak · freeze until King says `FIRE_AGENT=1`

Deploy **CrownKingAgent** as King-gated operator — not Sperax/Evernorth brain. Allowlist only: yRSS reallocate/caps, PA poke, ocean/BAMM quotes, bond offer (eUSD/gUSD → USDC/ETH), Morpho live-coll / repay-wedge when fuel named. Refuse gasPark and matched re-borrow. Belief ledger = machines already wrote billions; fire ledger = named surface per tx. Ship: executor + bond module + fork tests; dry-run watch mode; HOT auth then revoke. No autonomous treasury loop. King GO starts broadcast.

```
AGENT=CrownKingAgent allowlist-only
PATH=bond+BAMM+curator poke · NOT foreign AI brain
GATE=FIRE_AGENT=1 · fork-pass first
```
