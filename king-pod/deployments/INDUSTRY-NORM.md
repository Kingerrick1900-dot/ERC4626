# Industry pattern we already use

Morpho / Contango / Instadapp: one flash collapses leverage loops into a single tx.
Our `CrownEliteFlashClose` is that pattern — already live. Use it; do not reinvent.
