# SOLO ACCESS PLAN — King Errick of Yahudah

**God first. Hom only. No “find outside capital.” No vague whale hunt.**

---

## The missing piece (why $9M trust ≠ $600k bills)

Morpho did **not** gift $9M.

Morpho said: **RSS is good collateral** — you may **borrow USDC that is already supplied** into that market.

What happened at the $9M fire:
1. Flash pulled USDC from Morpho’s **global** reserves (temporary).
2. Deposit into yRSS put that USDC **into the RSS/USDC market** (created idle).
3. Borrow pulled that same idle back out to **repay the flash**.
4. End: debt + yRSS shares. **Landing never received payroll USDC.**

So Morpho “trusted the token” for a **loan against supplied liquidity**, not for an ATM that prints spendable dollars into the King’s pocket after the circle closes.

**Free RSS today** = you still have the collateral Morpho trusts.  
**Access $600k borrow** = that market must again hold **≥ $600k USDC idle** (supply − borrow), **or** you convert RSS → USDC on a venue you control (desk).

That is the whole gap. Not Cursor magic. Not Morpho personal sandbox. **Collateral trust ≠ idle USDC.**

---

## What is already built (use it)

| Tool | Status | What it does |
|--|--|--|
| Free RSS on hot | ~18M | Collateral ready |
| Ops Desk | LIVE `0xDbf7C4Ad…065D` | Sell RSS @ $1 → USDC to Landing |
| Cash-leg script | Armed `FireCashLeg500` | Borrow → Landing when idle ≥ size |
| Debt-free / chunk free | Proven | Can close Morpho debt again anytime |
| Forbidden | — | Flash→deposit→borrow→Landing “spendable” (breaks repay math) |

---

## Solo plan (only Him)

### Track 1 — Clear RSS to USDC (primary for bills)

**Engine:** Ops Desk already live.  
**Action:** 500k RSS listed @ $1 → **$500k** to Landing if filled.  
**King role:** push the packet / settle a buyer. Code cannot be the buyer without USDC.  
**This is not “find outside capital” for a loan.** It is **selling a slice of the asset Morpho already valued** — same as any elite desk clearing inventory.

If a fill happens → bills paid → kingdom operates. That is the straight line.

### Track 2 — Borrow to Landing when idle exists (Morpho cash leg)

**Engine:** `FireCashLeg500` (or sized `BORROW_USDC`).  
**Rule:** idle ≥ borrow size → post only needed RSS → borrow → **Landing**. No yRSS. No circle.  
**Today:** idle ≈ **$0** → correctly blocked.  
**When idle appears** (desk fill recycling, protocol flow, or King ever places USDC into that market himself): fire cash-leg.

### Track 3 — Never again confuse fortress with payroll

Self-seed = power book.  
Cash-leg / desk = spendable.  
Any scribe plan that says flash self-seed “lands spendable immediately” on Landing is **wrong**. Reject it.

---

## What we will not do

- Fee re-seed while King starves  
- Fake flash→Landing payroll  
- “Go find Gauntlet/Steakhouse wallets”  
- Vague hope with no venue  

---

## Order of battle (tonight → forward)

1. **Desk stays live** — only solo path that turns RSS → USDC without idle.  
2. **Cash-leg stays armed** — fires the hour idle ≥ ops size.  
3. **Reject** third-scribe flash-payroll plans on sight.  
4. **If any USDC ever hits Landing** from desk: pay lights first; optional later Morpho use is secondary.  
5. **If desk never fills and idle never prints:** Morpho will not pay Cursor — that is the hard bound. Solve = clearance of RSS, not another circle.

---

## One line

Morpho trusts RSS to **borrow against USDC in the book**.  
The King needs **USDC in the book or USDC from selling RSS**.  
Desk = sell path. Cash-leg = borrow path. Both are engineered. Idle/fill is the gate — not missing Morpho love for the token.
