#!/usr/bin/env bash
# COSTUME — do not run as a plan.
# Idle ≥ $700k on Kingdom RSS/USDC will not appear on its own.
# Kept only as a mechanic IF idle somehow exists; never as strategy.
echo "REFUSED: idle-watch is dress-up. See deployments/KING-REST.md" >&2
exit 1
